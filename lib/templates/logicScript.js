// تم إنشاؤه تلقائياً من index.html المعتمد - لا تُعدَّل يدوياً
export const logicScriptJs = "/* ======================= STATE & HELPERS ======================= */\nlet state = { monthId: MONTHS[0].id, weekId: \"all\", entity: \"all\", search: \"\" };\n\nfunction fmtPct(x){ const v=Math.round(x*1000)/10; return (Number.isInteger(v)?v:v.toFixed(1))+\"%\"; }\nfunction ringSVG(pct, size=64, stroke=8){\n  const r=(size-stroke)/2, c=2*Math.PI*r, off=c*(1-pct);\n  return `<div class=\"ring\"><svg width=\"${size}\" height=\"${size}\" viewBox=\"0 0 ${size} ${size}\">\n    <circle cx=\"${size/2}\" cy=\"${size/2}\" r=\"${r}\" stroke=\"#f0e3e7\" stroke-width=\"${stroke}\" fill=\"none\"/>\n    <circle cx=\"${size/2}\" cy=\"${size/2}\" r=\"${r}\" stroke=\"#8a1538\" stroke-width=\"${stroke}\" fill=\"none\"\n      stroke-dasharray=\"${c}\" stroke-dashoffset=\"${off}\" stroke-linecap=\"round\"/>\n  </svg><div class=\"ring-val\">${fmtPct(pct)}</div></div>`;\n}\nfunction icon(path){ return `<svg width=\"22\" height=\"22\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.8\" stroke-linecap=\"round\" stroke-linejoin=\"round\">${path}</svg>`; }\nconst ICONS = {\n  chat: icon('<path d=\"M21 11.5a8.38 8.38 0 0 1-8.5 8.5c-1.4 0-2.7-.3-3.9-.9L3 21l1.9-5.6A8.38 8.38 0 0 1 3 11.5 8.38 8.38 0 0 1 11.5 3 8.38 8.38 0 0 1 21 11.5Z\"/>'),\n  users: icon('<path d=\"M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2\"/><circle cx=\"9\" cy=\"7\" r=\"4\"/><path d=\"M23 21v-2a4 4 0 0 0-3-3.87\"/><path d=\"M16 3.13a4 4 0 0 1 0 7.75\"/>'),\n  bank: icon('<path d=\"M3 21h18\"/><path d=\"M3 10h18\"/><path d=\"M5 6l7-3 7 3\"/><path d=\"M4 10v11\"/><path d=\"M20 10v11\"/><path d=\"M8 14v3\"/><path d=\"M12 14v3\"/><path d=\"M16 14v3\"/>'),\n  member: icon('<path d=\"M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2\"/><circle cx=\"9\" cy=\"7\" r=\"4\"/><path d=\"M22 21v-2a4 4 0 0 0-3-3.87\"/><path d=\"m16 3.13 1 1 3-3\"/>'),\n  check: icon('<path d=\"M9 11l3 3L22 4\"/><path d=\"M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11\"/>'),\n  bulb: icon('<path d=\"M9 18h6\"/><path d=\"M10 22h4\"/><path d=\"M12 2a7 7 0 0 0-4 12.7c.5.4.9 1.1.9 1.8V17h6.2v-.5c0-.7.4-1.4.9-1.8A7 7 0 0 0 12 2Z\"/>'),\n  practice: icon('<rect x=\"3\" y=\"4\" width=\"18\" height=\"17\" rx=\"2\"/><path d=\"M8 2v4\"/><path d=\"M16 2v4\"/><path d=\"M3 10h18\"/>'),\n  vote: icon('<path d=\"M20 6 9 17l-5-5\"/>'),\n  compass: icon('<circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"m16 8-4 4-4 4 4-4 4-4Z\"/>'),\n  warn: icon('<path d=\"M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0Z\"/><path d=\"M12 9v4\"/><path d=\"M12 17h.01\"/>'),\n};\n\nfunction computeAvailableEntities(){\n  return Array.from(new Set(PLATFORM_ENTITIES)).sort();\n}\nfunction populateEntityFilter(){\n  const sel = document.getElementById('entityFilter');\n  computeAvailableEntities().forEach(e=>{\n    const o = document.createElement('option'); o.value=e; o.textContent=e; sel.appendChild(o);\n  });\n}\nfunction textMatches(str){\n  if(!state.search) return true;\n  return (str||\"\").toLowerCase().includes(state.search.toLowerCase());\n}\nfunction orgMatches(org){\n  if(state.entity===\"all\") return true;\n  if(!org) return false;\n  if(Array.isArray(org)) return org.includes(state.entity);\n  return org===state.entity;\n}\n\n/* نطاق الأسابيع الحالي: إما كل أسابيع الشهر المختار (عرض \"الشهر كامل\")\n   أو أسبوع واحد بعينه. هذا يعمم weeksFor القديمة لأي عدد أسابيع مستقبلاً. */\nfunction currentScopeKey(){ return state.weekId===\"all\" ? state.monthId : state.weekId; }\nfunction weekIdsInScope(scopeKey){\n  if(isMonthId(scopeKey)) return monthOf(scopeKey).weekIds.slice();\n  return [scopeKey];\n}\n\nfunction getFilteredMembers(scopeKey, entity){\n  const weekIds = weekIdsInScope(scopeKey);\n  return MEMBERS\n    .map(m=>{\n      const val = weekIds.reduce((s,wid)=> s + (m.weeks[wid]||0), 0);\n      return {name:m.name, org:m.org, part:m.part, inter:m.inter, total:m.total, val};\n    })\n    .filter(m=> entity===\"all\" || m.org===entity);\n}\nfunction weeksWithEntityActivity(scopeKey, entity){\n  const weekIds = weekIdsInScope(scopeKey);\n  if(entity===\"all\") return weekIds;\n  return weekIds.filter(wid=> MEMBERS.some(m=> m.org===entity && (m.weeks[wid]||0)>0));\n}\nfunction countWord(n, singular, dual){\n  if(n===0) return \"0\";\n  if(n===1) return singular;\n  if(n===2) return dual;\n  return String(n);\n}\nfunction computeKPI(scopeKey, entity){\n  const filtered = getFilteredMembers(scopeKey, entity);\n  const activeList = filtered.filter(m=>m.val>0);\n  const active = activeList.length;\n  const qualitative = activeList.reduce((s,m)=>s+m.val,0);\n\n  let entitiesCount, rate;\n  if(entity===\"all\"){\n    entitiesCount = KPI[scopeKey].entities; // قيمة أساس معتمدة لكل نطاق\n    rate = active / RATE_DENOMINATORS[scopeKey];\n  } else {\n    entitiesCount = new Set(activeList.map(m=>m.org)).size;\n    const totalForEntity = MEMBERS.filter(m=>m.org===entity).length;\n    rate = totalForEntity>0 ? active/totalForEntity : 0;\n  }\n  const rateDenomLabel = entity===\"all\" ? RATE_DENOMINATORS[scopeKey] : MEMBERS.filter(m=>m.org===entity).length;\n\n  const practices = entity===\"all\" ? KPI[scopeKey].practices : filterRecords(PRACTICES, scopeKey, entity).length;\n  const ideas     = entity===\"all\" ? KPI[scopeKey].ideas     : filterRecords(IDEAS, scopeKey, entity).length;\n  const outputs   = entity===\"all\" ? KPI[scopeKey].outputs   : filterFlatRecords(OUTPUTS, scopeKey, entity, \"orgs\").length;\n  const challenges= entity===\"all\" ? KPI[scopeKey].challenges: filterRecords(CHALLENGES, scopeKey, entity).length;\n\n  const activeWeeks = weeksWithEntityActivity(scopeKey, entity);\n\n  return {qualitative, rate, rateDenomLabel, active, entities:entitiesCount, practices, ideas, outputs, challenges, activeWeeks};\n}\nfunction filterRecords(dataByWeek, scopeKey, entity, orgField){\n  orgField = orgField || \"org\";\n  const weekIds = weekIdsInScope(scopeKey);\n  let out = [];\n  weekIds.forEach(wid=> (dataByWeek[wid]||[]).forEach(item=> out.push({...item, weekId:wid})) );\n  out = out.filter(item=> orgMatches(item[orgField]));\n  out = out.filter(item=> textMatches(JSON.stringify(item)));\n  return out;\n}\nfunction filterFlatRecords(list, scopeKey, entity, orgField){\n  orgField = orgField || \"org\";\n  const weekIds = weekIdsInScope(scopeKey);\n  return list\n    .filter(item=> weekIds.includes(item.weekId))\n    .filter(item=> orgMatches(item[orgField]))\n    .filter(item=> textMatches(JSON.stringify(item)));\n}\n\n/* ======================= RENDER: PAGE 1 ======================= */\nfunction kpiCard(iconHtml, value, label, sub){\n  return `<div class=\"kpi-card\"><div class=\"kpi-icon\">${iconHtml}</div><div class=\"kpi-value\">${value}</div><div class=\"kpi-label\">${label}</div>${sub?`<div class=\"kpi-sub\">${sub}</div>`:\"\"}</div>`;\n}\nfunction ringCard(pct, label, sub){\n  return `<div class=\"kpi-card\">${ringSVG(pct)}<div class=\"kpi-label\">${label}</div>${sub?`<div class=\"kpi-sub\">${sub}</div>`:\"\"}</div>`;\n}\n\nfunction renderPage1(){\n  const scopeKey = currentScopeKey();\n  const entity = state.entity;\n  const k = computeKPI(scopeKey, entity);\n  const month = monthOf(state.monthId);\n  let title, sub;\n\n  if(state.weekId===\"all\"){\n    title = `الملخص التنفيذي`;\n    sub = `نظرة شاملة على أداء المنصة خلال الشهر.`;\n  } else {\n    const topic = TOPICS[scopeKey];\n    title = `الملخص التنفيذي – ${topic}`;\n    sub = `نظرة على أداء المنصة خلال هذا الأسبوع (${DATES[scopeKey]}).`;\n  }\n  if(entity!==\"all\"){\n    sub += ` — مرشّح حسب: ${entity}`;\n  }\n\n  const activeWeekCount = k.activeWeeks.length;\n  const topicsValue = countWord(activeWeekCount, \"محور واحد\", \"محوران\");\n  const roundsValue = countWord(activeWeekCount, \"جولة واحدة\", \"جولتان\");\n\n  const row1 = [\n    kpiCard(ICONS.users, KPI[month.id].totalMembers, \"إجمالي أعضاء المنصة\"),\n    kpiCard(ICONS.bank, KPI[scopeKey].entitiesOnPlatform, \"الجهات الممثلة في المنصة\"),\n    kpiCard(ICONS.bank, k.entities, \"الجهات المتفاعلة\"),\n    kpiCard(ICONS.member, k.active, \"الأعضاء المتفاعلون\"),\n    ringCard(k.rate, \"نسبة الأعضاء المتفاعلين\", `من أصل ${k.rateDenomLabel} عضوًا`),\n    kpiCard(ICONS.chat, k.qualitative, \"إجمالي التفاعل\"),\n  ];\n  const row2 = [\n    kpiCard(ICONS.compass, topicsValue, \"المحاور المطروحة\"),\n    kpiCard(ICONS.vote, roundsValue, \"جولات التصويت\"),\n    kpiCard(ICONS.practice, k.practices, \"الممارسات والتجارب الحكومية\"),\n    kpiCard(ICONS.bulb, k.ideas, \"الأفكار والمقترحات\"),\n    kpiCard(ICONS.check, k.outputs, \"المخرجات القابلة للمتابعة\"),\n  ];\n\n  document.getElementById('p1-title').textContent = title;\n  document.getElementById('p1-sub').textContent = sub;\n  document.getElementById('p1-row1').innerHTML = row1.join(\"\");\n  document.getElementById('p1-row2').innerHTML = row2.join(\"\");\n\n  const body = document.getElementById('p1-exec-body');\n\n  if(entity!==\"all\"){\n    document.getElementById('p1-exec-title').textContent = `ملخص الجهة: ${entity}`;\n    if(k.active===0){\n      body.innerHTML = `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n    } else {\n      const periodLabel = state.weekId===\"all\" ? \"الشهر\" : \"هذا الأسبوع\";\n      const lines = [\n        `شارك ${k.active} من أعضاء «${entity}» خلال ${periodLabel}، بإجمالي ${k.qualitative} تفاعل.`,\n        `سجلت الجهة ${k.practices} ممارسة وتجربة حكومية و${k.ideas} فكرة ومقترح ضمن الفترة المحددة.`,\n        `رُصد للجهة ${k.outputs} مخرج قابل للمتابعة خلال الفترة نفسها.`\n      ];\n      body.innerHTML = `<ul style=\"margin:0;padding-inline-start:20px;\">${lines.map(t=>`<li>${t}</li>`).join(\"\")}</ul>`;\n    }\n    return;\n  }\n\n  document.getElementById('p1-exec-title').textContent = state.weekId===\"all\" ? \"الملخص التنفيذي للشهر\" : \"أبرز ما خرج به النقاش\";\n  if(state.weekId===\"all\"){\n    body.innerHTML = `<ul style=\"margin:0;padding-inline-start:20px;\">${EXEC_SUMMARY[month.id].map(t=>`<li>${t}</li>`).join(\"\")}</ul>`;\n  } else {\n    body.innerHTML = `<div class=\"nugget-list\">${HIGHLIGHTS_DETAILED[scopeKey].map((item,i)=>`\n      <div class=\"nugget-item\">\n        <div class=\"nugget-num\">${String(i+1).padStart(2,'0')}</div>\n        <div class=\"nugget-body\">\n          <div class=\"nugget-title\">${item.title}</div>\n          <div class=\"nugget-desc\">${item.desc}</div>\n        </div>\n      </div>`).join(\"\")}</div>`;\n  }\n}\n\n/* ======================= RENDER: PAGE 2 ======================= */\nfunction periodMembers(scopeKey){\n  return getFilteredMembers(scopeKey, state.entity)\n    .filter(m=>m.val>0)\n    .filter(m=>textMatches(m.name)||textMatches(m.org))\n    .sort((a,b)=>b.val-a.val);\n}\nfunction periodEntities(scopeKey){\n  const filtered = getFilteredMembers(scopeKey, state.entity).filter(m=>m.val>0);\n  const byOrg = {};\n  filtered.forEach(m=>{ byOrg[m.org] = (byOrg[m.org]||0) + m.val; });\n  return Object.keys(byOrg)\n    .map(name=>({name, val:byOrg[name]}))\n    .filter(e=>textMatches(e.name))\n    .sort((a,b)=>b.val-a.val);\n}\n\nfunction renderPage2(){\n  const scopeKey = currentScopeKey();\n  const month = monthOf(state.monthId);\n  document.getElementById('p2-sub').textContent = 'عرض مستوى تفاعل الأعضاء وتوزيعه بين الجهات.';\n\n  const members = periodMembers(scopeKey);\n  document.getElementById('p2-members').innerHTML = members.length ? `<table><thead><tr><th>العضو</th><th>الجهة</th><th>إجمالي التفاعل</th></tr></thead><tbody>` +\n    members.slice(0,10).map(m=>`<tr><td>${m.name}</td><td><span class=\"chip\">${m.org}</span></td><td><b>${m.val}</b></td></tr>`).join(\"\") +\n    `</tbody></table>` : `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n\n  const entities = periodEntities(scopeKey);\n  const maxE = Math.max(...entities.map(e=>e.val), 1);\n  document.getElementById('p2-entities').innerHTML = entities.length ? entities.map(e=>`\n    <div class=\"bar-row\">\n      <div class=\"bar-label\">${e.name}</div>\n      <div class=\"bar-track\"><div class=\"bar-fill\" style=\"width:${(e.val/maxE*100).toFixed(0)}%\"></div></div>\n      <div class=\"bar-val\">${e.val}</div>\n    </div>`).join(\"\") : `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n\n  // مقارنة الأسابيع: تُبنى ديناميكياً من أسابيع الشهر المختار (أي عدد، وليس 3 فقط)\n  const wrap = document.getElementById('p2-compare-wrap');\n  if(state.weekId===\"all\"){\n    const rows = month.weekIds.map(wid=>({label:weekLabel(wid), k:computeKPI(wid, state.entity)}));\n    const maxQ = Math.max(...rows.map(r=>r.k.qualitative), 1);\n    const maxR = Math.max(...rows.map(r=>r.k.rate), 0.0001);\n    wrap.style.display=\"\";\n    if(rows.every(r=>r.k.qualitative===0)){\n      document.getElementById('p2-compare').innerHTML = `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n    } else {\n      document.getElementById('p2-compare').innerHTML = `\n        <div>\n          <h3 style=\"font-size:14px;\">إجمالي التفاعل</h3>\n          ${rows.map(r=>`<div class=\"bar-row\"><div class=\"bar-label\">${r.label}</div><div class=\"bar-track\"><div class=\"bar-fill\" style=\"width:${(r.k.qualitative/maxQ*100).toFixed(0)}%\"></div></div><div class=\"bar-val\">${r.k.qualitative}</div></div>`).join(\"\")}\n        </div>\n        <div>\n          <h3 style=\"font-size:14px;\">نسبة التفاعل</h3>\n          <p style=\"font-size:11.5px; color:var(--ink-soft); margin:0 0 10px;\">تُحسب نسبة التفاعل وفق عدد أعضاء المنصة خلال الفترة المحددة، لذا قد يختلف مقام النسبة بين الأسابيع.</p>\n          ${rows.map(r=>`<div class=\"bar-row\"><div class=\"bar-label\">${r.label}</div><div class=\"bar-track\"><div class=\"bar-fill\" style=\"width:${(r.k.rate/maxR*100).toFixed(0)}%\"></div></div><div class=\"bar-val\">${fmtPct(r.k.rate)}</div></div>`).join(\"\")}\n        </div>`;\n    }\n  } else {\n    wrap.style.display=\"none\";\n  }\n}\n\n/* ======================= RENDER: PAGE 3 ======================= */\nfunction renderPage3(){\n  const scopeKey = currentScopeKey();\n  const month = monthOf(state.monthId);\n  document.getElementById('p3-sub').textContent = 'أبرز الممارسات والتجارب والرؤى والمقترحات والتحديات التي خرجت بها النقاشات خلال الشهر.';\n\n  const EMPTY = `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n\n  const pr = filterRecords(PRACTICES, scopeKey, state.entity);\n  document.getElementById('p3-practices').innerHTML = pr.length ? pr.map(x=>`\n    <div class=\"practice-item\">\n      <div class=\"o\">${x.orgDisplay||x.org}</div>\n      <div class=\"t\">${x.name}</div>\n      <div class=\"d\">${x.desc}</div>\n    </div>`).join(\"\") : EMPTY;\n\n  // أبرز ما خرج به النقاش لعرض الشهر كامل: أقوى رؤية من كل أسبوع ضمن الشهر (يتوسع تلقائياً\n  // مع أي عدد أسابيع مستقبلاً، دون الحاجة لتعديل يدوي).\n  const hlItems = state.weekId===\"all\"\n    ? month.weekIds.map(wid=> HIGHLIGHTS_DETAILED[wid][0])\n    : HIGHLIGHTS_DETAILED[scopeKey];\n  document.getElementById('p3-highlights').innerHTML = hlItems.map((item, i)=>`\n    <div class=\"nugget-item\">\n      <div class=\"nugget-num\">${String(i+1).padStart(2,'0')}</div>\n      <div class=\"nugget-body\">\n        <div class=\"nugget-title\">${item.title}</div>\n        <div class=\"nugget-desc\">${item.desc}</div>\n      </div>\n    </div>`).join(\"\");\n\n  const id = filterRecords(IDEAS, scopeKey, state.entity);\n  document.getElementById('p3-ideas').innerHTML = id.length ? id.map(x=>`\n    <div class=\"practice-item\">\n      <div class=\"o\">${x.by ? `${x.by} — ${x.orgDisplay||x.org}` : \"فكرة جماعية من النقاش\"}</div>\n      <div class=\"t\">${x.title}</div>\n      <div class=\"d\">${x.desc}</div>\n    </div>`).join(\"\") : EMPTY;\n\n  const ch = filterRecords(CHALLENGES, scopeKey, state.entity);\n  document.getElementById('p3-challenges').innerHTML = ch.length ? ch.map(c=>`\n    <div class=\"practice-item\">\n      <div class=\"t\">${c.t}</div>\n      <div class=\"d\">${c.by ? `${c.by} — ${c.orgDisplay||c.org}` : (c.note||\"\")}</div>\n    </div>`).join(\"\") : EMPTY;\n\n  let q = [];\n  weekIdsInScope(scopeKey).forEach(wid=> (QUOTES[wid]||[]).forEach(x=> q.push(x)) );\n  document.getElementById('p3-quotes').innerHTML = q.map(x=>`\n    <div class=\"quote-card\"><p>\"${x.q}\"</p><div class=\"who\">${x.who}</div><div class=\"role\">${x.role}</div></div>`).join(\"\");\n}\n\n/* ======================= RENDER: PAGE ARCHIVE (أرشيف المنصة) ======================= */\nfunction renderArchive(){\n  const scopeKey = currentScopeKey();\n  const weekIds = weekIdsInScope(scopeKey);\n  const month = monthOf(state.monthId);\n  document.getElementById('p4-sub').textContent = state.weekId===\"all\"\n    ? `حصاد أسابيع الشهر، والملفات والمراجع المرتبطة بالنقاشات.`\n    : `حصاد هذا الأسبوع، والملفات والمراجع المرتبطة به.`;\n\n  const harvest = HARVEST_FILES.filter(f=>weekIds.includes(f.weekId));\n  document.getElementById('p4-harvest').innerHTML = harvest.map(f=>`\n    <div class=\"file-card\">\n      <div class=\"file-icon\">${ICONS.check}</div>\n      <div class=\"file-meta\">\n        <div class=\"name\">${f.title}</div>\n        <div class=\"sub\">المحور: ${TOPICS[f.weekId]} · ${DATES[f.weekId]}</div>\n      </div>\n      <a class=\"file-open\" href=\"${f.href}\">فتح / تنزيل الملف</a>\n    </div>`).join(\"\");\n\n  const ENTITY_EMPTY = `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n  const NO_FILES_YET = `<div class=\"empty-note\">لا توجد ملفات أو مراجع إضافية مسجلة لهذا الأسبوع بعد.</div>`;\n  function refCard(f){\n    return `<div class=\"file-card\">\n      <div class=\"file-icon\">${ICONS.chat}</div>\n      <div class=\"file-meta\">\n        <div class=\"name\">${f.title}</div>\n        <div class=\"sub\">${f.by} — ${f.orgDisplay||f.org}</div>\n      </div>\n      ${f.available ? `<a class=\"file-open\" href=\"${f.href}\">فتح / تنزيل الملف</a>` : `<span class=\"file-unavailable\">الملف الأصلي غير متوفر</span>`}\n    </div>`;\n  }\n  // قسم مراجع لكل أسبوع ضمن النطاق الحالي — يُبنى ديناميكياً، يتوسع تلقائياً لأي عدد أسابيع.\n  const container = document.getElementById('p4-refs-container');\n  container.innerHTML = weekIds.map(wid=>{\n    const allFiles = REF_FILES[wid] || [];\n    const textFiltered = allFiles.filter(f=>textMatches(JSON.stringify(f)));\n    const entityFiltered = textFiltered.filter(f=>orgMatches(f.org));\n    let body;\n    if(allFiles.length===0) body = NO_FILES_YET;\n    else if(entityFiltered.length===0) body = ENTITY_EMPTY;\n    else body = entityFiltered.map(refCard).join(\"\");\n    return `<div class=\"card\">\n      <h3>📎 ملفات ومراجع ${weekLabel(wid)} — ${TOPICS[wid]}</h3>\n      <div>${body}</div>\n    </div>`;\n  }).join(\"\");\n}\n\n/* ======================= RENDER: PAGE OUTPUTS (المخرجات والمتابعة) ======================= */\nfunction renderOutputs(){\n  const scopeKey = currentScopeKey();\n  const month = monthOf(state.monthId);\n  document.getElementById('p5-sub').textContent = state.weekId===\"all\"\n    ? 'أبرز المخرجات التي تم رصدها خلال الشهر لمتابعتها مع الجهات المعنية.'\n    : 'أبرز المخرجات التي تم رصدها خلال هذا الأسبوع لمتابعتها مع الجهات المعنية.';\n\n  const items = filterFlatRecords(OUTPUTS, scopeKey, state.entity, \"orgs\");\n  document.getElementById('p5-outputs').innerHTML = items.length ? items.map((o,i)=>`\n    <div class=\"out-item\">\n      <div class=\"out-num\">${i+1}</div>\n      <div>\n        <div class=\"out-text\">${o.text}</div>\n      </div>\n    </div>`).join(\"\") : `<div class=\"empty-note\">لا توجد بيانات مسجلة للجهة خلال الفترة المحددة.</div>`;\n}\n\n/* ======================= RENDER ALL / EVENTS ======================= */\nfunction renderAll(){\n  renderPage1(); renderPage2(); renderPage3(); renderArchive(); renderOutputs();\n}\n\ndocument.getElementById('nav').addEventListener('click', e=>{\n  const item = e.target.closest('.nav-item');\n  if(!item) return;\n  document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));\n  item.classList.add('active');\n  document.querySelectorAll('.page').forEach(pg=>pg.classList.remove('active'));\n  document.getElementById('page-'+item.dataset.page).classList.add('active');\n  closeMobileNav();\n});\n\n/* ---------------- قائمة التنقل على الجوال (☰) — تستخدم نفس عناصر ووظيفة الـsidebar أعلاه ---------------- */\nconst sidebarEl = document.querySelector('.sidebar');\nconst sidebarBackdrop = document.getElementById('sidebarBackdrop');\nfunction openMobileNav(){ sidebarEl.classList.add('open'); sidebarBackdrop.classList.add('open'); }\nfunction closeMobileNav(){ sidebarEl.classList.remove('open'); sidebarBackdrop.classList.remove('open'); }\ndocument.getElementById('mobileMenuBtn').addEventListener('click', e=>{ e.stopPropagation(); openMobileNav(); });\ndocument.getElementById('sidebarCloseBtn').addEventListener('click', closeMobileNav);\nsidebarBackdrop.addEventListener('click', closeMobileNav);\n\n/* ---------------- شريط الفلترة: شهر ثم أسبوع، مبنيان ديناميكياً من MONTHS/WEEKS ---------------- */\nconst monthSelect = document.getElementById('monthSelect');\nconst monthMenu = document.getElementById('monthMenu');\nconst monthLabelEl = document.getElementById('monthSelectLabel');\nconst weekSelect = document.getElementById('weekSelect');\nconst weekMenu = document.getElementById('weekMenu');\nconst weekLabelEl = document.getElementById('weekSelectLabel');\n\nfunction populateMonthMenu(){\n  monthMenu.innerHTML = MONTHS.map(m=>`<div class=\"period-option${m.id===state.monthId?' selected':''}\" data-month=\"${m.id}\">${m.label}</div>`).join(\"\");\n  monthLabelEl.textContent = monthOf(state.monthId).label;\n}\nfunction populateWeekMenu(){\n  const month = monthOf(state.monthId);\n  const opts = [`<div class=\"period-option${state.weekId===\"all\"?' selected':''}\" data-week=\"all\">الشهر كامل</div>`]\n    .concat(month.weekIds.map(wid=>`<div class=\"period-option${wid===state.weekId?' selected':''}\" data-week=\"${wid}\">${weekLabel(wid)}</div>`));\n  weekMenu.innerHTML = opts.join(\"\");\n  weekLabelEl.textContent = state.weekId===\"all\" ? \"الشهر كامل\" : weekLabel(state.weekId);\n}\n\nmonthSelect.addEventListener('click', e=>{\n  e.stopPropagation();\n  weekMenu.classList.remove('open'); weekSelect.classList.remove('open');\n  monthMenu.classList.toggle('open');\n  monthSelect.classList.toggle('open');\n});\nmonthMenu.addEventListener('click', e=>{\n  const opt = e.target.closest('.period-option');\n  if(!opt) return;\n  state.monthId = opt.dataset.month;\n  state.weekId = \"all\"; // العودة لعرض الشهر كامل عند تبديل الشهر\n  populateMonthMenu();\n  populateWeekMenu();\n  monthMenu.classList.remove('open');\n  monthSelect.classList.remove('open');\n  renderAll();\n});\nweekSelect.addEventListener('click', e=>{\n  e.stopPropagation();\n  monthMenu.classList.remove('open'); monthSelect.classList.remove('open');\n  weekMenu.classList.toggle('open');\n  weekSelect.classList.toggle('open');\n});\nweekMenu.addEventListener('click', e=>{\n  const opt = e.target.closest('.period-option');\n  if(!opt) return;\n  state.weekId = opt.dataset.week;\n  populateWeekMenu();\n  weekMenu.classList.remove('open');\n  weekSelect.classList.remove('open');\n  renderAll();\n});\ndocument.addEventListener('click', ()=>{\n  monthMenu.classList.remove('open'); monthSelect.classList.remove('open');\n  weekMenu.classList.remove('open'); weekSelect.classList.remove('open');\n});\n\ndocument.getElementById('entityFilter').addEventListener('change', e=>{\n  state.entity = e.target.value;\n  renderAll();\n});\n\ndocument.getElementById('searchInput').addEventListener('input', e=>{\n  state.search = e.target.value.trim();\n  renderAll();\n});\n\npopulateEntityFilter();\npopulateMonthMenu();\npopulateWeekMenu();\nrenderAll();\n\n";
